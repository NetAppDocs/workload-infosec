---
sidebar: sidebar
permalink: ai-tool-boundaries.html
keywords: infosec, security, data handling, data retention, data collection, permissions, credentials, iam, assume role, least privilege, compliance, soc 2, govcloud, amazon bedrock, lambda, link, fsx for ontap, logging, audit, access control, onboarding, hardening
summary: "NetApp Workload Factory takes information security and data protection practices seriously, focusing on data-handling practices and access requirements with guidance for least-privilege configurations and potential hardening."
---

= AI tool boundaries for NetApp Workload Factory
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
AWS Bedrock uses the AWS CLI and ONTAP CLI diagnostic tools in a read-only manner.
Data is only used transiently for diagnostic purposes and is not retained by NetApp Workload Factory or AWS.

== Tool safety controls

[cols="2,3,4",options="header"]
|===
|Tool |Capability |Safety controls
|AWS CLI (read-only) |Execute read-only AWS CLI commands (describe, list, get) |Blocks all mutating verbs (create, delete, modify, update, put, remove). Maximum 10 commands per step. Output truncated at 20 KB.
|ONTAP CLI (read-only) |Execute read-only ONTAP CLI commands using SSH |Blocks all mutating verbs (delete, destroy, create, modify, move). Output truncated.
|===

The agent cannot modify, create, or delete resources. It can only observe and diagnose.

== Data retention and model training boundaries

Per AWS policy, Amazon Bedrock does not store or use your inputs and outputs to train or improve foundation models.
For on-demand inference (used by Workload Factory), your data is processed transiently and not retained by AWS after the response is returned.