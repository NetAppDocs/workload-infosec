---
sidebar: sidebar
permalink: least-privilege-requirements.html
keywords: least privilege, iam permissions, iam role, session policies, external id, confused deputy, sts, assumerole, tiered permissions, read-only, credential tiers, tag-based conditions, resource-level access, secrets manager, fsxsecret, permission validation, ask-me, arn scoping, security controls, aws credentials, cross-account
summary: "You can restrict Workload Factory IAM permissions at any time to align with least-privilege requirements."
---

= Learn about least privilege requirements for NetApp Workload Factory
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ../media/

[.lead]
You can restrict Workload Factory IAM permissions at any time to align with least-privilege requirements, including:

* Limiting access to specific resources (ARNs)
* Applying IAM conditions (for example, tags and CIDR ranges)

Workload Factory also provides an Ask Me advisory flow that helps you safely refine permissions by suggesting restriction options and generating an updated policy to apply in AWS.

This enables you to move from initial onboarding to securely scoped, resource-level access.

Workload Factory uses a tiered permission model aligned with least privilege. You choose which capability tiers to enable when adding AWS credentials. You can start with read-only discovery and expand as needed.

All permissions are granted through an IAM role in your AWS account that Workload Factory assumes via STS with an external ID. Each API call is further scoped with an inline session policy, ensuring only the minimum actions needed for that specific operation are used.

== Granting permissions

When link:https://docs.netapp.com/us-en/workload-setup-admin/add-credentials.html[adding AWS credentials to Workload Factory], you can choose which permission tiers to enable. Each capability has different tiers, and you can enable or disable tiers at any time. The tiers are cumulative; enabling a higher tier automatically enables all lower tiers.

Refer to link:https://docs.netapp.com/us-en/workload-setup-admin/permissions-reference.html[the Permissions reference] for IAM policies and details on the specific actions granted by each tier.

== Security controls

* *External ID*: Every cross-account role assumption requires a unique external ID to prevent confused deputy attacks.
* *Session policies*: Each operation uses an inline session policy that restricts temporary credentials to only the specific actions needed, even if the role grants broader permissions.
* *Tag-based conditions*: Security group modifications are restricted to Workload Factory-created resources only.
* *Secret ARN scoping*: Secrets Manager access is restricted to secrets matching the `FSxSecret*` naming pattern.
* *Credential tiers*: You control which tiers are enabled. Start with Tier 1 (read-only) and enable tiers 2 and 3 when ready.
* *Permission validation*: The system detects missing permissions at runtime and reports the specific action and resource that failed, enabling targeted remediation.

== Impacts of removing permissions 

=== Tier 1 (view, planning, and analysis) permissions

[cols="1,1,1",options="header"] 
|=== 
|Permission |Purpose |Impact If Removed 
 
|fsx:DescribeFileSystems 
|Discover and display file system configuration 
|No file systems visible 
 
|fsx:DescribeVolumes 
|List volumes and their properties 
|Volumes not displayed 
 
|fsx:DescribeStorageVirtualMachines 
|List storage VMs, protocols, DNS configuration 
|SVMs not shown 
 
|fsx:DescribeBackups 
|List available backups 
|Backup information unavailable 
 
|fsx:ListTagsForResource 
|Display resource tags 
|Tags missing 
 
|fsx:DescribeSharedVpcConfiguration 
|Detect shared VPC deployments 
|Shared VPC info unavailable 
 
|fsx:DescribeS3AccessPointAttachments 
|List S3 access points on volumes 
|S3 access point info missing 
 
|s3:GetAccessPoint 
|Retrieve S3 access point details 
|Access point details unavailable 
 
|cloudwatch:GetMetricData 
|Retrieve performance and capacity metrics 
|Dashboards and charts empty 
 
|cloudwatch:GetMetricStatistics 
|Capacity utilization monitoring 
|Capacity management features disabled 
 
|ec2:DescribeInstances 
|Discover compute workloads using FSx storage 
|Workload topology incomplete 
 
|ec2:DescribeVolumes 
|EBS volume discovery for storage comparison 
|Savings analysis incomplete 
 
|ec2:DescribeVpcs 
|VPC enumeration for network context 
|VPC selection unavailable 
 
|ec2:DescribeSubnets 
|Subnet enumeration for placement decisions 
|Subnet selection unavailable 
 
|ec2:DescribeSecurityGroups 
|Security group discovery and validation 
|Security group info missing 
 
|ec2:DescribeRouteTables 
|Network route mapping 
|Route information unavailable 
 
|ec2:DescribeNetworkInterfaces 
|Network interface discovery 
|Connectivity checks degraded 
 
|elasticfilesystem:DescribeFileSystems 
|EFS discovery for migration and savings analysis 
|EFS not included in savings 
 
|ce:GetCostAndUsage 
|Cost data for savings calculations 
|Cost analysis unavailable 
 
|ce:GetTags 
|Cost allocation tag queries 
|Tag-based cost views fail 
|=== 

Resource scope: All resources (*). These are read-only Describe/Get/List actions. 

=== Tier 2 (operations and remediation) permissions

The operations and remediation permissions are optional and enabled by default. Tier 2 permissions enable volume lifecycle, backup management, S3 access points, metadata catalog, and AI-powered event analysis.

Resource scope: All resources (`*`), except CloudTrail actions which are conditioned on the tag `netapp-fsx-s3-metadata=journal`.

NOTE: AI inference runs in your AWS account using your Bedrock inference profiles. No data is sent outside your account for AI features. 

[cols="2,4,3",options="header"]
|===
|Permission |Purpose |Impact if removed

|fsx:CreateVolume
|Create new volumes
|Volume creation unavailable

|fsx:DeleteVolume
|Delete volumes
|Volume deletion unavailable

|fsx:UpdateFileSystem
|Modify throughput and storage capacity
|File system updates and auto-capacity management disabled

|fsx:UpdateStorageVirtualMachine
|Modify SVM settings
|SVM configuration changes unavailable

|fsx:UpdateVolume
|Modify volume size, tiering, and QoS
|Volume modifications unavailable

|fsx:CreateBackup
|Create on-demand backups
|Manual backup creation unavailable

|fsx:CreateVolumeFromBackup
|Restore a volume from backup
|Restore from backup unavailable

|fsx:DeleteBackup
|Remove backups
|Backup deletion unavailable

|fsx:TagResource / fsx:UntagResource
|Manage FSx resource tags
|Tagging operations fail

|fsx:CreateAndAttachS3AccessPoint
|Create an S3 access point on a volume
|S3 access point creation unavailable

|fsx:DetachAndDeleteS3AccessPoint
|Remove an S3 access point from a volume
|S3 access point removal unavailable

|s3:CreateAccessPoint / s3:DeleteAccessPoint
|S3 access point lifecycle
|S3 access point operations fail

|s3:TagResource / s3:UntagResource / s3:ListTagsForResource
|S3 resource tagging
|S3 tag operations fail

|s3:GetObjectTagging / s3:ListBucket
|Query metadata catalog content
|Metadata catalog queries degraded

|bedrock:InvokeModel / bedrock:InvokeModelWithResponseStream
|AI-powered event analysis and diagnostics
|AI analysis features unavailable

|bedrock:ListInferenceProfiles / bedrock:GetInferenceProfile
|Enumerate available AI models
|Cannot configure AI features

|s3tables:CreateTableBucket / s3tables:CreateTable / s3tables:CreateNamespace
|Create metadata catalog structures
|Metadata catalog creation fails

|s3tables:GetTable / s3tables:GetTableBucket / s3tables:GetNamespace / s3tables:GetTableMetadataLocation
|Read metadata catalog configuration
|Metadata catalog reads fail

|s3tables:PutTableData / s3tables:GetTableData
|Write and read metadata catalog entries
|Metadata catalog operations fail

|s3tables:ListTables / s3tables:ListNamespaces / s3tables:ListTableBuckets
|Enumerate metadata catalog objects
|Metadata catalog navigation fails

|s3tables:UpdateTableMetadataLocation
|Update catalog metadata pointers
|Catalog location updates fail

|s3tables:ListTagsForResource / s3tables:TagResource
|Metadata catalog tagging
|Catalog tag operations fail

|cloudtrail:PutEventSelectors / cloudtrail:GetEventSelectors
|Metadata catalog change journaling
|Catalog journaling unavailable
|===

Resource scope: All resources (`*`), except CloudTrail actions which are conditioned on the tag `netapp-fsx-s3-metadata=journal`.

NOTE: AI inference runs in your AWS account using your Bedrock inference profiles. No data is sent outside your account for AI features. 

=== Tier 3 (file system creation) permissions

The file system creation permissions are optional. Tier 3 permissions enable provisioning and deprovisioning of FSx for ONTAP file systems and storage VMs.

[cols="1,1,1",options="header"] 
|=== 
|Permission |Purpose |Impact If Removed 
 
|fsx:CreateFileSystem 
|Provision new FSx for ONTAP file systems 
|File system creation unavailable 
 
|fsx:DeleteFileSystem 
|Delete file systems 
|File system deletion unavailable 
 
|fsx:CreateStorageVirtualMachine 
|Create storage VMs 
|SVM creation unavailable 
 
|fsx:DeleteStorageVirtualMachine 
|Delete storage VMs 
|SVM deletion unavailable 
 
|fsx:TagResource / fsx:UntagResource 
|Tag resources during creation 
|Created resources untagged 
 
|kms:CreateGrant 
|Allow FSx to use your KMS key for encryption at rest 
|Encrypted file system creation fails 
 
|kms:DescribeKey / kms:ListKeys / kms:ListAliases 
|Enumerate available KMS keys for selection 
|KMS key picker empty 
 
|iam:CreateServiceLinkedRole 
|Create the FSx service-linked role (one-time) 
|First file system creation fails if role doesn't exist 
 
|ec2:CreateSecurityGroup / ec2:CreateTags 
|Create security group for new file system 
|File system creation fails 
 
|ec2:DescribeVolumeStatus 
|Verify underlying storage health 
|Health checks degraded 
 
|ec2:AuthorizeSecurityGroupEgress / Ingress 
|Configure security group rules for ONTAP access 
|Network rules not applied 
 
|ec2:RevokeSecurityGroupEgress / Ingress 
|Clean up security group rules on deletion 
|Stale rules remain 
 
|ec2:DeleteSecurityGroup 
|Remove security group on file system deletion 
|Orphaned security groups 
|=== 

Resource scope: Security group modification actions (Authorize, Revoke, Delete) are restricted to resources tagged AppCreator=NetappFSxWF — only security groups created by Workload Factory can be modified or deleted. 



// 10-JUNE-2026 migrate onboarding guide