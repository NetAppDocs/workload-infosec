---
sidebar: sidebar
permalink: connectivity-trust-paths.html
keywords: infosec, security, data handling, data retention, data collection, permissions, credentials, iam, assume role, least privilege, compliance, soc 2, govcloud, amazon bedrock, lambda, link, fsx for ontap, logging, audit, access control, onboarding, hardening
summary: "NetApp Workload Factory takes information security and data protection practices seriously, focusing on data-handling practices and access requirements with guidance for least-privilege configurations and potential hardening."
---

= Connectivity and trust paths for NetApp Workload Factory
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]

Learn about cross-system connectivity paths and trust boundaries for NetApp Workload Factory network and IAM integration.

== Connectivity paths

=== Workload Factory to AWS APIs

Used for `DescribeFileSystems`, `DescribeVolumes`, `CreateVolume`, `UpdateVolume`, `DeleteVolume`,
`CreateFileSystem`, `CreateBackup`, `DescribeBackups`, and EC2/VPC APIs for subnet and security group discovery.

* Collector service scans about every 5 hours.
* CRUD operations run on demand.
* All calls use STS-assumed temporary credentials with an external ID.

The following diagram shows how Workload Factory discovers and monitors using read-only AWS API calls. 

image::workflow-1.png[Workflow 1: Discovering and monitoring (Read-only AWS API calls)]

=== Workload Factory to customer AWS account resources (orchestration and automation)


Includes:

* CloudFormation stack deployment for IAM role provisioning and FSx creation
* STS `AssumeRole` for cross-account access
* CloudWatch alarm creation for Lambda monitoring
* S3 presigned URL generation for CloudFormation templates
* `lambda:InvokeFunction` to trigger the Link Lambda

=== Lambda link to ONTAP management endpoint

Specific ONTAP-native operations can include:

* EMS event retrieval
* QoS and latency statistics
* Volume efficiency settings
* Aggregate capacity checks
* SVM management
* igroup/LUN operations
* anti-ransomware status checks
* SnapMirror configuration
* NFS client information
* cluster version queries

Protocols:

* HTTPS/443 (REST)
* SSH/22 (CLI)

Lambda link runs inside the customer VPC for private subnet access without exposing ONTAP publicly.

=== Lambda link to AWS Secrets Manager (ONTAP credential retrieval)

Used only when ONTAP admin credentials are stored in AWS Secrets Manager (alternative: credentials encrypted in Workload Factory using envelope encryption).

* The proxy-forwarder passes `x-aws-secret-arn` (Base64-encoded) in headers.
* Lambda link calls `GetSecretValue` at execution time to retrieve the password.

This keeps the password within your account boundary and prevents transmission from Workload Factory.

=== Workload Factory and customer components to Amazon Bedrock (AI diagnostics)

Used only when AI diagnostics are enabled.

* Event Analyzer uses this path for EMS analysis and latency diagnostics.
* Bedrock runs with your assumed credentials and inference profile ARN, so data does not flow to NetApp’s account.

Data sent to Bedrock can include EMS event JSON, QoS statistics, volume configuration, aggregate data, and node information.
The path is active only when an inference profile is configured per organization (`ai_analyzer_config` table).

== Connection groups (protocols, ports, and auth)

=== Group A — AWS management plane trust path (assumed credentials)

[cols="2,1,1,2,2,3",options="header"]
|===
|Connection |Protocol |Port |Direction |Auth |Justification
|STS AssumeRole |HTTPS |443 |WF → AWS STS (customer account) |IAM credentials + ExternalId |Obtain temporary cross-account credentials
|FSx API (Describe/Create/Update/Delete) |HTTPS |443 |WF → AWS FSx endpoint (customer region) |STS temporary credentials |File system and volume lifecycle management
|EC2/VPC API |HTTPS |443 |WF → AWS EC2 endpoint |STS temporary credentials |Security group, subnet, and VPC discovery
|CloudFormation API |HTTPS |443 |WF → AWS CFN endpoint |STS temporary credentials |Stack deployment for IAM roles and FSx provisioning
|Secrets Manager GetSecretValue |HTTPS |443 |WF → AWS Secrets Manager endpoint (customer account) |STS temporary credentials |Retrieve ONTAP admin password (when Secrets Manager-stored)
|KMS Encrypt/Decrypt |HTTPS |443 |WF → AWS KMS endpoint |STS temporary credentials |Volume encryption key operations
|===

All AWS API calls use the customer's regional endpoint and can be routed through VPC endpoints if configured.

=== Group B — ONTAP data plane

The ONTAP data plane is always proxied through Lambda link; Workload Factory services never connect directly to ONTAP.

[cols="2,1,1,2,2,3",options="header"]
|===
|Connection |Protocol |Port |Direction |Auth |Justification
|ONTAP REST API |HTTPS |443 |Link (customer VPC) → ONTAP management LIF |Basic auth (username/password) or Secrets Manager ARN |Cluster, volume, and SVM configuration; QoS; EMS events; ARP status
|ONTAP SSH CLI |SSH |22 |Link (customer VPC) → ONTAP management LIF |Password auth |Diagnostic commands (QoS stats, df, event logs, efficiency)
|===

Routing strategy (priority order) for the proxy-forwarder:

. Explicit `x-link-id` header: look up Lambda ARN from the database
. Target ID (FSx file system ID): find the associated link
. Agent/Connector ID: route through the message broker to the Connector

=== Group C — AWS Lambda link (deployed in your VPC)

[cols="2,1,1,2,2,3",options="header"]
|===
|Connection |Protocol |Port |Direction |Auth |Justification
|Lambda Invoke |HTTPS (AWS SDK) |443 |WF → AWS Lambda API (customer account) |IAM (`lambda:InvokeFunction`) |Execute ONTAP REST/SSH commands from within customer VPC
|Lambda → ONTAP |HTTPS + SSH |443, 22 |Lambda (customer VPC) → ONTAP management LIF |Basic auth / password |Private subnet access to ONTAP without public exposure
|===

=== Group D — Connector/NetApp Console Agent (EC2 in your VPC, outbound-only)

[cols="2,1,1,2,2,3",options="header"]
|===
|Connection |Protocol |Port |Direction |Auth |Justification
|Agent polling |HTTPS |443 |Connector (customer VPC) → WF message broker |Bearer token (occm-access scope) |Long-poll for pending ONTAP commands (7-second timeout; reconnects)
|Agent response |HTTPS |443 |Connector (customer VPC) → WF message broker |Bearer token |Return ONTAP command results (optionally zlib-compressed)
|Connector → ONTAP |HTTPS + SSH |443, 22 |Connector (customer VPC) → ONTAP management LIF |Basic auth / password |Execute proxied ONTAP operations
|===

Key design: Workload Factory never initiates inbound connections to the Connector.
Work is queued in Redis streams; the Connector polls `GET /messagebroker/v1/requests` and responds with `POST /messagebroker/v1/responses`.

=== Group E — CloudFormation custom resource callbacks

[cols="2,1,1,2,2,3",options="header"]
|===
|Connection |Protocol |Port |Direction |Auth |Justification
|CFN → WF Lambda |HTTPS |443 |CloudFormation (customer) → WF Lambda (NetApp) |JWT token + reference token |Report IAM role creation status and pass back role ARN
|ONTAP CFN Resource Provider → Link |HTTPS |443 |CFN resource Lambda (customer) → Link Lambda (customer) |IAM |Create/update/delete ONTAP resources during stack operations
|===

== Summary: network requirements for your VPC

[cols="2,1,3",options="header"]
|===
|Component |Inbound |Outbound
|FSx for ONTAP |Port 443 and 22 from Link Lambda or Connector security group |N/A
|Link Lambda |None |Port 443 (ONTAP, AWS APIs) and port 22 (ONTAP SSH)
|Connector EC2 |None |Port 443 (WF endpoints, ONTAP, AWS APIs) and port 22 (ONTAP SSH)
|VPC endpoints (optional) |N/A |STS, FSx, S3, Secrets Manager, Lambda, CloudFormation
|===

No inbound internet access is required for any component in the customer VPC.
All connections are either outbound-initiated (Connector polling) or invoked through AWS service APIs (Lambda Invoke).