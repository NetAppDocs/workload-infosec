---
sidebar: sidebar
permalink: amazon-bedrock-opt-in.html
keywords: infosec, security, data handling, data retention, data collection, permissions, credentials, iam, assume role, least privilege, compliance, soc 2, govcloud, amazon bedrock, lambda, link, fsx for ontap, logging, audit, access control, onboarding, hardening
summary: "NetApp Workload Factory takes information security and data protection practices seriously, focusing on data-handling practices and access requirements with guidance for least-privilege configurations and potential hardening."
---

= Amazon Bedrock opt-in for NetApp Workload Factory AI diagnostics
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
AI diagnostics require IAM permissions, an inference profile, and connectivity controls before they can be enabled.

AI diagnostics are disabled by default.

== Before you begin

* Make sure your account includes an AWS Bedrock inference profile.
* Have a connected Lambda link with SSH capability for diagnostic data collection.

== Steps

. Grant Bedrock permissions in your IAM role:
+
* `bedrock:InvokeModel`
* `bedrock:InvokeModelWithResponseStream`
* `bedrock:ListInferenceProfiles`
* `bedrock:GetInferenceProfile`

. Configure the inference profile ARN in Workload Factory settings.

If any prerequisite is missing, the system reports the specific missing component and AI features remain inactive.

== Disable AI diagnostics

To disable AI diagnostics:

* Remove the Bedrock permissions from your IAM role, or
* Delete the Bedrock configuration in Workload Factory settings.

AI diagnostics become unavailable immediately with no residual data processing.

== Region and cross-region inference profiles

Bedrock inference executes in the AWS region specified by your inference profile.
If you use a cross-region inference profile, AWS might route the request to any region in the profile’s configuration per standard AWS Bedrock behavior.
Data does not leave AWS infrastructure.

== AI-powered event analysis using Amazon Bedrock

The following diagram shows the data flow for AI-powered event analysis using Amazon Bedrock.

image::workflow-3.png[Workflow 3: AI-powered event analysis (Amazon Bedrock)]