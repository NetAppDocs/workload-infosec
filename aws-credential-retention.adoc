---
sidebar: sidebar
permalink: aws-credential-retention.html
keywords: credential retention, aws credentials, iam role, role arn, external id, sts, temporary credentials, access key, session token, redis, cache, ttl, mysql, encryption, aes-256-cbc, envelope encryption, kms, ontap credentials, ontap password, secrets manager, session policies, least privilege, confused deputy, govcloud, china, standard accounts, credential validation, credential deletion, data retention
summary: "How Workload Factory keeps credentials secure."
---

= Workload Factory AWS credential retention
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ../media/

[.lead]
Workload Factory secures credential handling through short-lived AWS tokens, encrypted role metadata, and bounded retention behavior:

This page is the primary reference for credential and token retention behavior.

* No long-term AWS access keys are ever stored. Workload Factory stores only a pointer (role ARN) and a shared secret (external ID). Neither grants AWS access on its own.
* Temporary credentials have a hard maximum lifetime of 1 hour (AWS-enforced), and Workload Factory caches them for at most 30 minutes before forcing a fresh STS call.
* If a cached token has fewer than 15 minutes remaining, it is discarded and a new token is obtained, ensuring operations never fail mid-execution due to credential expiry.
* Credential deletion by the customer immediately revokes Workload Factory's ability to access that account (the next AssumeRole call will fail, and cached tokens expire within minutes).

== Retention and storage summary

[cols="2,2,3,1,2",options="header"]
|===
|Data |Storage location |Retention duration |Auto-expires? |Deletion method

|IAM role ARN + external ID
|MySQL (encrypted at rest)
|Indefinite (stored until customer explicitly removes)
|No
|Customer clicks "Delete credential" in the UI

|STS temporary credentials (access key + secret + session token)
|Redis (in-memory cache)
|Maximum 30 minutes (cache TTL). Underlying token is valid for up to 1 hour (AWS default). Cache evicts 5 minutes before token expiry as a safety margin.
|Yes (Redis TTL auto-evicts; AWS token expires regardless of cache state)
|Automatic (no action required)

|ONTAP admin password
|MySQL (AES-256-CBC envelope encryption using KMS)
|Indefinite (stored until the customer removes the credential or deletes the file system)
|No
|Customer deletes the credential, or file system deletion triggers cleanup

|Decrypted ONTAP password (plaintext)
|In-memory only (never written to disk or cache)
|Duration of a single request (milliseconds)
|Yes (garbage collected after the request completes)
|Automatic (never persisted)
|===

== Security controls

Workload Factory includes the following security controls:

* External ID prevents confused deputy attacks.
* Session policies enforce per-request least privilege.
* Temporary credentials are short-lived and refreshed before risky expiry windows.
* Regional and account-type handling differs for Standard, GovCloud, and China environments.
* Workload Factory never stores long-term AWS access keys.
* Workload Factory never modifies your IAM role's permissions.
* Workload Factory never escalates beyond the permissions granted by your role policy.
* Session policies can only reduce permissions; Workload Factory never expands them.

== ONTAP credential handling references

For ONTAP credential handling details, refer to the primary references:

* Storage option comparison and GovCloud requirement: link:credential-considerations.html[Considerations for Workload Factory credentials^]
* Split credential model by operation type: link:credential-types.html[Learn about Workload Factory credential types^]

Credential validity is periodically rechecked, and invalid credentials are flagged in the UI.


// 10-JUNE-2026 migrate onboarding guide